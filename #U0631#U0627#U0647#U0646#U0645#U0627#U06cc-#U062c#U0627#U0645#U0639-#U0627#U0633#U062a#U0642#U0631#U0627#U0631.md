# راهنمای جامع استقرار سایت رسا تیم روی هاست اشتراکی

این فایل حاصل ادغام سه راهنمای قبلی پروژه (`README-نصب.md`، `DEPLOY-GUIDE-EMAIL.md`، `DEPLOY-GUIDE-LOGS-CLEANUP.md`) است که همه در یک مسیر واحد و مرحله‌به‌مرحله کنار هم قرار گرفته‌اند. مسیر پیشنهادی این است که از بالا تا پایین، بخش‌ها را به ترتیب انجام دهید.

> 💡 این فایل خودش با پسوند `.md` است و طبق `.htaccess` ریشه‌ی پروژه، دسترسی مستقیم مرورگر به فایل‌های `.md` بسته شده — پس حتی اگر آن را روی هاست هم بگذارید کسی نمی‌تواند بازش کند. با این حال پیشنهاد می‌شود بعد از اتمام کار، این فایل را از روی هاست پاک کنید و فقط نسخه‌ی محلی‌اش را نگه دارید.

---

## فهرست مطالب

1. [پیش‌نیازها](#۱-پیش‌نیازها)
2. [خرید/آماده‌سازی هاست و اتصال دامنه](#۲-خرید-آماده-سازی-هاست-و-اتصال-دامنه)
3. [ساخت دیتابیس MySQL](#۳-ساخت-دیتابیس-mysql)
4. [تنظیم فایل config.php](#۴-تنظیم-فایل-configphp)
5. [آپلود فایل‌های پروژه](#۵-آپلود-فایل‌های-پروژه)
6. [ساخت اولین حساب ادمین و حذف پوشه‌ی نصب](#۶-ساخت-اولین-حساب-ادمین-و-حذف-پوشه‌ی-نصب)
7. [آشنایی با پنل مدیریت (rasa-manager)](#۷-آشنایی-با-پنل-مدیریت-rasa-manager)
8. [فعال‌سازی کامل بخش ایمیل (فاز ۱ تا ۴)](#۸-فعال‌سازی-کامل-بخش-ایمیل-فاز-۱-تا-۴)
9. [تنظیم SPF / DKIM / DMARC](#۹-تنظیم-spf--dkim--dmarc)
10. [راه‌اندازی کرون پاک‌سازی لاگ‌ها](#۱۰-راه‌اندازی-کرون-پاک‌سازی-لاگ‌ها)
11. [نقشه‌ی سایت، سئو و robots.txt](#۱۱-نقشه‌ی-سایت-سئو-و-robotstxt)
12. [حالت تعمیرات](#۱۲-حالت-تعمیرات)
13. [پشتیبان‌گیری و بازیابی](#۱۳-پشتیبان‌گیری-و-بازیابی)
14. [چک‌لیست نهایی امنیت قبل از انتشار](#۱۴-چک‌لیست-نهایی-امنیت-قبل-از-انتشار)
15. [رفع اشکالات رایج](#۱۵-رفع-اشکالات-رایج)

---

## ۱. پیش‌نیازها

- یک هاست اشتراکی با پشتیبانی از **PHP 8** یا بالاتر (اکثر هاست‌های ایرانی/خارجی مبتنی بر cPanel این را دارند)
- یک دیتابیس **MySQL** قابل ساخت از پنل هاست
- فعال بودن افزونه‌ی **GD** برای PHP (برای نمایش کپچای تصویری فرم تماس — روی تقریباً همه‌ی هاست‌های اشتراکی به‌صورت پیش‌فرض فعال است)
- دسترسی به **cPanel** (این راهنما برای پنل هاست از نوع cPanel نوشته شده؛ اگر پنل هاست شما DirectAdmin یا چیز دیگری است، مراحل مشابه‌اند ولی نام منوها فرق می‌کند)
- یک دامنه (مثلاً `rasateams.ir`) که یا از همان شرکت هاست خریده‌اید یا جای دیگری ثبت شده و می‌خواهید آن را به این هاست وصل کنید

---

## ۲. خرید/آماده‌سازی هاست و اتصال دامنه

### الف) اگر دامنه و هاست را از یک شرکت خریده‌اید
معمولاً به‌صورت خودکار به هم وصل می‌شوند و نیازی به کار دستی نیست. اگر بعد از خرید هنوز سایت بالا نیامد، چند ساعت صبر کنید (انتشار DNS).

### ب) اگر دامنه از یک‌جا و هاست از جای دیگر است
دو روش برای وصل کردن دامنه به هاست وجود دارد؛ **فقط یکی** را انجام دهید:

**روش ۱ — تغییر Nameserver (پیشنهادی و ساده‌تر)**
1. از پنل هاست (ایمیل خوش‌آمدگویی هاست یا داخل cPanel) نام‌سرورها (Nameserver) را پیدا کنید؛ معمولاً چیزی شبیه:
   ```
   ns1.hostname.com
   ns2.hostname.com
   ```
2. وارد پنل ثبت دامنه‌ی خود شوید (جایی که دامنه را خریده‌اید).
3. بخش **Nameservers / DNS Management** دامنه را پیدا کنید و نام‌سرورهای هاست را جایگزین نام‌سرورهای فعلی کنید.
4. ذخیره کنید. انتشار تغییرات معمولاً بین ۱ تا ۲۴ ساعت (گاهی تا ۴۸ ساعت) طول می‌کشد.

**روش ۲ — فقط رکورد A (وقتی می‌خواهید DNS دامنه همچنان جای دیگری مثل Cloudflare مدیریت شود)**
1. آی‌پی سرور هاست را از cPanel یا ایمیل خوش‌آمدگویی هاست پیدا کنید.
2. در پنل مدیریت DNS دامنه (مثلاً Cloudflare)، یک رکورد از نوع **A** برای `@` (ریشه‌ی دامنه) و یکی هم برای `www` بسازید که به همان آی‌پی اشاره کند.
3. اگر از Cloudflare استفاده می‌کنید و می‌خواهید مستقیم SSL/CDN خودش را روی سایت فعال کند، ابر نارنجی (Proxy) را فعال بگذارید؛ در غیر این‌صورت روی حالت DNS Only (ابر خاکستری) بگذارید تا گواهی SSL خودِ هاست بدون تداخل صادر شود.

> ⚠️ اگر با cPanel هاست فقط یک دامنه (Primary Domain) دارید، دامنه‌ی نهایی سایت را همان زمان تحویل هاست مشخص کرده‌اید. اگر دامنه‌ی اضافه (Addon Domain) اضافه می‌کنید، از cPanel → **Domains** گزینه‌ی **Create A New Domain** را بزنید و پوشه‌ی ریشه (Document Root) آن را به همان پوشه‌ای که فایل‌های سایت را آپلود می‌کنید اشاره دهید (مثلاً `public_html`).

### فعال‌سازی SSL (HTTPS)
بعد از این‌که دامنه به هاست وصل شد و کمی صبر کردید:
1. وارد cPanel شوید → بخش **SSL/TLS Status** یا **SSL/TLS** (روی اکثر هاست‌های امروزی چیزی به نام **AutoSSL** یا **Let's Encrypt** هست).
2. دامنه‌ی خود و `www.` را انتخاب و **Run AutoSSL** را بزنید. معمولاً چند دقیقه طول می‌کشد.
3. بعد از فعال شدن SSL، حتماً آدرس سایت را با `https://` تست کنید. (فایل `.htaccess` پروژه هدر `Strict-Transport-Security` دارد که HTTPS را برای مرورگر اجباری می‌کند، پس بعد از فعال‌سازی SSL دیگر مشکلی برای ریدایرکت به HTTPS ندارید — فقط مطمئن شوید گواهی واقعاً صادر شده باشد وگرنه سایت با خطای گواهی بالا نمی‌آید.)

---

## ۳. ساخت دیتابیس MySQL

1. وارد cPanel شوید → بخش **MySQL® Databases**.
2. زیر «Create New Database» یک نام دیتابیس وارد کنید (مثلاً `resa_db`) و بسازید. cPanel معمولاً یک پیشوند به اسم کاربری هاست جلوی اسم دیتابیس می‌گذارد (مثل `username_resa_db`) — همین اسم کامل را باید در `config.php` بگذارید.
3. پایین همان صفحه، زیر «MySQL Users» یک کاربر جدید با رمز عبور **قوی** بسازید (از دکمه‌ی Password Generator هاست استفاده کنید و رمز را جایی امن ذخیره کنید).
4. زیر «Add User to Database»، همان کاربر را به دیتابیسی که ساختید اضافه کنید و در صفحه‌ی بعد گزینه‌ی **All Privileges** را تیک بزنید و ذخیره کنید.
5. نام کامل دیتابیس، نام کامل کاربر و رمز عبور را یادداشت کنید — در مرحله‌ی بعد لازم‌شان دارید.

> **نیازی به اجرای دستی هیچ فایل SQL نیست.** جدول‌های دیتابیس در اولین بار که سایت/پنل ادمین باز می‌شود به‌صورت خودکار ساخته می‌شوند.

---

## ۴. تنظیم فایل config.php

فایل `config.php` (در ریشه‌ی پروژه) را با یک ویرایشگر متن ساده باز کنید و مقادیر زیر را با اطلاعات واقعی خودتان جایگزین کنید.

### اتصال دیتابیس (اجباری)
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'نام کامل دیتابیسی که ساختید');
define('DB_USER', 'نام کامل کاربر دیتابیس');
define('DB_PASS', 'رمز عبوری که ساختید');
```

### امنیت نشست (اجباری)
```php
define('APP_SECRET', 'یک رشته‌ی طولانی و کاملاً تصادفی جایگزین این متن کن');
```
یک رشته‌ی حداقل ۴۰-۵۰ کاراکتری تصادفی (حروف، عدد، نماد) بگذارید. هرگز مقدار پیش‌فرض را نگه ندارید.

می‌توانید تعداد تلاش‌های مجاز ورود و مدت قفل‌شدن حساب را هم عوض کنید (پیش‌فرض‌ها معقول‌اند و معمولاً نیازی به تغییر نیست):
```php
define('LOGIN_MAX_ATTEMPTS', 5);
define('LOGIN_LOCK_SECONDS', 900); // ۱۵ دقیقه
```

### منطقه‌ی زمانی
پیش‌فرض روی `Asia/Tehran` تنظیم شده و نیازی به تغییر ندارد مگر بخواهید سایت را برای کاربران منطقه‌ی زمانی دیگری اجرا کنید.

### بازه‌ی نگه‌داری لاگ‌ها (اختیاری)
```php
define('SITE_VISITS_RETENTION_DAYS', 7);
define('ACTIVITY_LOGS_RETENTION_DAYS', 30);
```
این دو عدد را کرون پاک‌سازی لاگ‌ها (بخش ۱۰) استفاده می‌کند؛ در همین مرحله می‌توانید عوضشان کنید یا بعداً.

### تنظیمات مربوط به ایمیل ادمین‌ها (اختیاری — فقط اگر می‌خواهید بخش ایمیل را فعال کنید)
اگر همین الان قصد فعال‌سازی کامل ایمیل را ندارید می‌توانید این بخش را فعلاً دست‌نخورده رها کنید؛ کل سایت و پنل ادمین بدون خطا کار می‌کنند و فقط یک پیام هشدار ساده در تب «ایمیل» پنل نشان داده می‌شود. برای فعال‌سازی کامل، بخش ۸ همین راهنما را ببینید — آنجا دقیقاً همین مقادیر را با جزئیات کامل توضیح می‌دهیم:
```php
define('CPANEL_HOST', 'yourdomain.ir');
define('CPANEL_PORT', 2083);
define('CPANEL_USERNAME', '');
define('CPANEL_API_TOKEN', '');

define('MAIL_DOMAIN', 'rasateams.ir');
define('MAIL_QUOTA_MB', 300);
define('MAIL_ATTACHMENT_MAX_MB', 10);
define('MAIL_ATTACHMENT_RETENTION_DAYS', 30);
define('MAIL_WEBMAIL_URL', 'https://mail.rasateams.ir/resa-mail-bridge.php');
define('MAIL_SSO_TOKEN_TTL', 60);
define('MAIL_IMAP_HOST', 'localhost');
define('MAIL_IMAP_PORT', 993);
define('MAIL_ENCRYPTION_KEY', 'change-this-to-a-second-long-random-string-0987654321');
```
⚠️ `MAIL_ENCRYPTION_KEY` هم باید مثل `APP_SECRET` قبل از انتشار به یک رشته‌ی تصادفی جدید و متفاوت تغییر کند (این دو مقدار نباید یکسان باشند).

> نکته: چون کد پروژه به‌صورت خودکار تشخیص می‌دهد که روی `localhost`/دامنه‌ی `.local`/`.test` است یا روی یک هاست واقعی (بر اساس نام دامنه‌ای که مرورگر با آن درخواست می‌زند)، نمایش خطاهای PHP روی صفحه به محض بالا رفتن روی دامنه‌ی واقعی خودش خاموش می‌شود — نیازی به تنظیم دستی این بخش نیست.

---

## ۵. آپلود فایل‌های پروژه

1. از طریق **File Manager** داخل cPanel یا یک نرم‌افزار FTP (مثل FileZilla) به هاست وصل شوید.
2. تمام محتویات این پوشه (شامل فایل‌های مخفی مثل `.htaccess` — در FileZilla از منوی Server گزینه‌ی «Force showing hidden files» را فعال کنید تا این فایل‌ها هم منتقل شوند) را داخل پوشه‌ی ریشه‌ی سایت (معمولاً `public_html`، یا برای Addon Domain همان پوشه‌ای که موقع ساخت دامنه مشخص کردید) آپلود کنید.
3. بعد از آپلود کامل، مطمئن شوید فایل `.htaccess` ریشه واقعاً منتقل شده (چون خیلی از کلاینت‌های FTP به‌صورت پیش‌فرض فایل‌های نقطه‌دار را مخفی می‌کنند و ممکن است جا بمانند).
4. دسترسی (Permission) پوشه‌ها معمولاً باید `755` و فایل‌ها `644` باشد؛ اکثر هاست‌ها بعد از آپلود این مقدار را خودکار درست تنظیم می‌کنند. اگر خطای دسترسی دیدید، این دو مقدار را از File Manager چک/تنظیم کنید. پوشه‌ی `assets/uploads` و `rasa-manager/storage/backups` باید توسط PHP قابل نوشتن باشند (برای آپلود تصویر و پشتیبان‌گیری) — اگر خطای «امکان ذخیره تصویر نبود» یا مشابه گرفتید، دسترسی همین دو پوشه را بررسی کنید.

---

## ۶. ساخت اولین حساب ادمین و حذف پوشه‌ی نصب

1. بعد از آپلود کامل و درست بودن `config.php`، به این آدرس بروید:
   ```
   https://دامنه-شما.ir/install/install.php
   ```
2. یک نام کاربری و رمز عبور قوی برای اولین حساب سوپرادمین وارد کنید و فرم را ارسال کنید.
3. **بلافاصله بعد از ساخت موفق اولین ادمین، پوشه‌ی `install` را کامل از روی هاست پاک کنید** (از File Manager انتخاب کنید و Delete بزنید). این پوشه فقط تا زمانی‌که هیچ ادمینی در سیستم نباشد کار می‌کند؛ اما برای امنیت بیشتر بهتر است اصلاً روی هاست باقی نماند. (اگر فراموش کنید و بعداً دوباره سر بزنید، خودِ اسکریپت هم به‌صورت خودکار یک `.htaccess` با `Require all denied` داخل همان پوشه می‌سازد تا قفل بماند — ولی حذف کامل پوشه همیشه ایمن‌تر است.)

---

## ۷. آشنایی با پنل مدیریت (rasa-manager)

آدرس ورود به پنل مدیریت:
```
https://دامنه-شما.ir/rasa-manager/login.php
```

با همان نام کاربری/رمزی که در مرحله‌ی نصب ساختید وارد شوید. تب‌های موجود در پنل (بسته به سطح دسترسی حساب شما — سوپرادمین همه‌ی تب‌ها را می‌بیند):

- **داشبورد** — آمار کلی، بازدیدها و وضعیت سلامت سیستم
- **پیام‌ها** — پیام‌های رسیده از فرم تماس؛ امکان خواندن، علامت‌گذاری خوانده‌شده/نشده و حذف
- **پروژه‌ها** — افزودن/ویرایش/حذف نمونه‌کارها (Case Study) که در صفحه‌ی «پروژه‌ها»ی سایت نمایش داده می‌شوند
- **وبلاگ‌ها** — نوشتن و مدیریت مقالات وبلاگ (با ویرایشگر متن غنی و پیش‌نمایش)
- **نظرات** — تأیید، پاسخ یا حذف نظرات زیر مقالات
- **ایمیل** — مدیریت ایمیل اختصاصی ادمین‌ها روی دامنه (نیاز به راه‌اندازی طبق بخش ۸)
- **تنظیمات** (فقط برای سطوح دسترسی مربوطه):
  - **پروفایل و امنیت** — اطلاعات شخصی، شبکه‌های اجتماعی، تغییر رمز عبور
  - **ادمین‌ها** — افزودن/ویرایش/حذف حساب‌های ادمین دیگر و سطح دسترسی هرکدام
  - **کارت اعضای تیم** — مدیریت کارت‌های بخش «تیم ما»ی سایت
  - **دستاوردها و جوایز** — تصاویر بخش دستاوردها
  - **افراد توسعه‌دهنده** — انتخاب ادمین‌هایی که در صفحه‌ی تماس با ما نمایش داده می‌شوند
  - **سایت** — تأیید نظرات و فعال/غیرفعال‌سازی «حالت تعمیرات»
  - **آمار و گزارش** — آمار بازدید و لاگ فعالیت‌ها
  - **پشتیبان‌گیری** — تهیه و بازیابی نسخه‌ی پشتیبان (بخش ۱۳)

نکته: بلافاصله بعد از اولین ورود موفق به پنل، جدول‌های اضافی مربوط به ایمیل (`admin_mailboxes` و `mail_cleanup_log`) نیز به‌صورت خودکار در دیتابیس ساخته می‌شوند؛ کافی است یک‌بار پنل را باز کرده باشید.

---

## ۸. فعال‌سازی کامل بخش ایمیل (فاز ۱ تا ۴)

این بخش کاملاً اختیاری است — اگر نمی‌خواهید برای هر ادمین یک ایمیل اختصاصی روی دامنه بسازید، می‌توانید کل این بخش را رد کنید؛ بقیه‌ی سایت بدون مشکل کار می‌کند و فقط تب «ایمیل» در پنل یک پیام راهنما نشان می‌دهد.

اگر می‌خواهید فعال کنید، این ویژگی از ۴ فاز تشکیل شده که فقط روی هاست واقعی (نه لوکال) قابل تست کامل هستند، چون به یک سرور mail واقعی و Cron Job واقعی نیاز دارند:

| فاز | چیست | کجا قابل تست است |
|---|---|---|
| ۱ | ساخت خودکار mailbox روی cPanel وقتی ادمین جدید ساخته می‌شود | فقط روی هاست واقعی |
| ۲ | نصب و شخصی‌سازی وب‌میل Roundcube (فارسی/RTL، محدودیت حجم) | روی هاست |
| ۳ | تب «ایمیل» در پنل ادمین + ورود خودکار بدون پسورد (SSO) | فقط با یک Roundcube واقعی و متصل |
| ۴ | کرون شبانه‌ی پاک‌سازی پیوست‌های قدیمی‌تر از ۳۰ روز | فقط روی هاست (نیاز به Cron Job واقعی) |

### مرحله ۱ — اتصال به cPanel (فاز ۱)
1. داخل cPanel: **Security → Manage API Tokens → Create** یک توکن جدید بسازید (نیازی به دسترسی WHM/ریسلر نیست، روی هاست اشتراکی معمولی هم کار می‌کند).
2. در `config.php` مقادیر زیر را پر کنید:
   ```php
   define('CPANEL_HOST', 'دامنه یا آدرس هاست شما بدون https://');
   define('CPANEL_USERNAME', 'یوزرنیم اکانت cPanel (نه یوزرنیم ادمین سایت)');
   define('CPANEL_API_TOKEN', 'توکنی که ساختید');
   define('MAIL_DOMAIN', 'دامنه‌ی واقعی شما');
   ```
3. از پنل ادمین سایت، یک ادمین آزمایشی بسازید و ببینید پیام «ایمیل ساخته شد» را می‌گیرید یا نه.
4. از داخل cPanel → **Email Accounts** چک کنید که mailbox واقعاً ساخته شده باشد.
5. همان ادمین آزمایشی را حذف کنید و مطمئن شوید mailboxش هم از cPanel پاک شده است.

### مرحله ۲ — نصب Roundcube (فاز ۲)
1. از [roundcube.net/download](https://roundcube.net/download) آخرین نسخه را دانلود کنید.
2. یک ساب‌دامین بسازید، مثلاً `mail.دامنه-شما.ir` (از cPanel → **Subdomains**).
3. فایل‌های Roundcube را داخل پوشه‌ی همان ساب‌دامین آپلود کنید.
4. یک دیتابیس MySQL **جدا و مستقل از دیتابیس اصلی سایت** برای خودِ Roundcube بسازید (دقیقاً مثل بخش ۳ همین راهنما، ولی یک دیتابیس تازه).
5. آدرس `mail.دامنه-شما.ir/installer` را باز کنید و ویزارد نصب را کامل کنید. فایل `webmail-integration/config.inc.php.template` داخل پروژه را به‌عنوان مرجع نگه دارید — بخش‌های مهم تنظیمات Roundcube که به این پروژه مربوط می‌شود آنجا مشخص شده‌اند؛ بقیه‌ی تنظیمات پیش‌فرض Roundcube را دست‌نخورده رها کنید.
6. بعد از نصب موفق، پوشه‌ی `installer` را طبق توصیه‌ی خودِ Roundcube حذف کنید (نکته‌ی امنیتی استاندارد خودِ Roundcube، نه فقط این پروژه).
7. با یوزر/پسورد یکی از mailboxها (مثلاً `name@دامنه-شما.ir`) یک‌بار دستی وارد `mail.دامنه-شما.ir` شوید و مطمئن شوید ارسال/دریافت ایمیل کار می‌کند.

### مرحله ۳ — وصل کردن ورود خودکار SSO (فاز ۳)
1. فایل `webmail-integration/resa-mail-bridge.php` را کنار فایل `index.php` خودِ Roundcube (همان پوشه‌ای که Roundcube را در مرحله‌ی قبل آپلود کردید) کپی کنید.
2. داخل همان فایل، مقدار `RESA_MAIN_APP_PATH` را با مسیر واقعی پوشه‌ی پروژه‌ی اصلی روی هاست جایگزین کنید (اگر مطمئن نیستید، از پشتیبانی هاست بپرسید یا از File Manager مسیر کامل را ببینید — چیزی شبیه `/home/username/دامنه-شما.ir`).
3. در `config.php` پروژه‌ی اصلی، مطمئن شوید `MAIL_WEBMAIL_URL` درست تنظیم شده:
   ```php
   define('MAIL_WEBMAIL_URL', 'https://mail.دامنه-شما.ir/resa-mail-bridge.php');
   ```
4. با یوزر ادمین خودتان وارد پنل شوید، تب «ایمیل» را بزنید — باید خودکار و بدون درخواست پسورد وارد Roundcube شوید.
5. اگر کار نکرد: مستقیم آدرس `mail.دامنه-شما.ir/?_task=login` را باز کنید و ببینید صفحه‌ی لاگین معمولی Roundcube می‌آید یا نه. اگر نیاید، مشکل از خودِ نصب Roundcube است نه از SSO.

### مرحله ۴ — کرون پاک‌سازی پیوست‌های ایمیل (فاز ۴)
1. از cPanel → **Cron Jobs**، یک ردیف جدید بسازید که هر شب ساعت ۳ بامداد اجرا شود:
   ```
   0 3 * * *  /usr/bin/php /home/USERNAME/دامنه-شما.ir/cron/mail-cleanup-cron.php
   ```
   (مسیر دقیق PHP را از پشتیبانی هاست بپرسید؛ معمولاً `/usr/bin/php` یا چیزی شبیه `/opt/cpanel/ea-php83/root/usr/bin/php`)
2. یک‌بار دستی از طریق SSH یا Terminal داخل cPanel اجرا کنید تا مطمئن شوید خطا نمی‌دهد:
   ```bash
   php /home/USERNAME/دامنه-شما.ir/cron/mail-cleanup-cron.php
   ```
3. بعد از هر اجرا، جدول `mail_cleanup_log` را از phpMyAdmin چک کنید که ردیف جدید ثبت شده باشد.
4. برای تست کامل: یک ایمیل تست با پیوست بفرستید و بعد از گذشت ۳۰ روز (یا با شبیه‌سازی تاریخ روی یک کپی جدا از دیتابیس، فقط برای تست) مطمئن شوید پیوستش حذف می‌شود ولی متن ایمیل باقی می‌ماند.

### نکته‌ی امنیتی مهم بعد از اتمام راه‌اندازی ایمیل
بعد از این‌که همه‌چیز را تست کردید:
- پوشه‌ی `webmail-integration/` را از روی هاست پاک کنید (یا حداقل خارج از پوشه‌ی عمومی/webroot نگه دارید) — فقط فایل `resa-mail-bridge.php` باید کنار خودِ Roundcube باشد، نه کل این پوشه کنار سایت اصلی.
- مطمئن شوید `MAIL_ENCRYPTION_KEY` و `APP_SECRET` در `config.php` هر دو با مقادیر تصادفی و متفاوت از پیش‌فرض جایگزین شده باشند.

---

## ۹. تنظیم SPF / DKIM / DMARC

این بخش هم فقط برای زمانی لازم است که بخش ایمیل (بخش ۸) را فعال کرده باشید. بدون این رکوردهای DNS، جیمیل/یاهو/آوت‌لوک با احتمال بالا ایمیل‌های ارسالی از دامنه‌ی شما را مستقیم داخل اسپم می‌اندازند یا رد می‌کنند، چون هیچ راهی برای اثبات این ندارند که سرور فرستنده واقعاً مجاز به ارسال از طرف این دامنه است. این کار فقط تنظیم DNS است و نیازی به تغییر کد ندارد.

1. از cPanel وارد بخش **Email → Email Deliverability** شوید.
2. کنار دامنه‌ی خودتان، اگر وضعیت «Invalid» یا هشدار قرمز نشان می‌دهد، روی **Manage** بزنید.
3. cPanel معمولاً خودش رکوردهای پیشنهادی SPF، DKIM و DMARC را می‌سازد — فقط دکمه‌ی **Install the Suggested DNS Records** را بزنید (اگر DNS دامنه از طریق همان هاست مدیریت می‌شود).
4. اگر DNS دامنه جای دیگری مدیریت می‌شود (مثلاً Cloudflare)، رکوردها را دستی همان‌جا اضافه کنید:
   - **SPF** (رکورد TXT روی خود دامنه): چیزی شبیه `v=spf1 +a +mx include:HOSTNAME_هاست ~all` (مقدار دقیق را از همان صفحه‌ی Email Deliverability کپی کنید)
   - **DKIM** (رکورد TXT روی `default._domainkey.دامنه-شما.ir`): مقدار کلید عمومی را دقیقاً از cPanel کپی کنید
   - **DMARC** (رکورد TXT روی `_dmarc.دامنه-شما.ir`): برای شروع یک سیاست ملایم کافی است، مثلاً:
     ```
     v=DMARC1; p=none; rua=mailto:postmaster@دامنه-شما.ir
     ```
     (بعد از چند هفته که مطمئن شدید همه‌چیز درست کار می‌کند، می‌توانید `p=none` را به `p=quarantine` سخت‌گیرانه‌تر تغییر دهید)
5. بعد از تنظیم، ۲۴ تا ۴۸ ساعت صبر کنید تا DNS منتشر شود، سپس با [mail-tester.com](https://www.mail-tester.com) یک ایمیل تست بفرستید و امتیاز را چک کنید (هدف: امتیاز ۹ یا ۱۰ از ۱۰).
6. یک ایمیل واقعی هم مستقیم به یک آدرس جیمیل بفرستید و مطمئن شوید داخل Inbox می‌آید نه پوشه‌ی Spam.

---

## ۱۰. راه‌اندازی کرون پاک‌سازی لاگ‌ها

این کرون (بر خلاف کرون بخش ۸) **اختیاری نیست و برای هر استقراری روی هاست پیشنهاد می‌شود**، چون جدول‌های آماری/لاگ سایت (نه ایمیل) را کنترل می‌کند تا با گذشت زمان بیش از حد بزرگ نشوند.

### این اسکریپت (`cron/logs-cleanup-cron.php`) دقیقاً چه کاری می‌کند؟

| جدول | چقدر نگه داشته می‌شود | چرا |
|---|---|---|
| `site_visits` | فقط ۷ روز اخیر | فقط برای نمودار «۷ روز اخیر» داشبورد پنل استفاده می‌شود |
| `activity_logs` | فقط ۳۰ روز اخیر | لاگ امنیتی/مدیریتی است، بازه‌ی طولانی‌تری دارد |

آمار همیشگی («تعداد کل بازدید» و «پربازدیدترین صفحات») از جدول جداگانه‌ی `page_view_totals` خوانده می‌شود که هیچ‌وقت پاک نمی‌شود — پس با اجرای این کرون آن آمارها گم نمی‌شوند.

> اگر خواستید بازه‌ی نگه‌داری را تغییر دهید، کافی است دو مقدار زیر را در `config.php` عوض کنید (بخش ۴ همین راهنما) — نیازی به دست‌زدن به کد کرون نیست:
> ```php
> define('SITE_VISITS_RETENTION_DAYS', 7);
> define('ACTIVITY_LOGS_RETENTION_DAYS', 30);
> ```

### مرحله ۱: پیدا کردن مسیر PHP روی هاست
وارد **cPanel → Terminal** شوید (یا از طریق SSH وصل شوید) و این دستور را بزنید:
```bash
which php
```
معمولاً یکی از این‌هاست:
```
/usr/bin/php
/usr/local/bin/php
/opt/cpanel/ea-php82/root/usr/bin/php
```
این مسیر را یادداشت کنید.

### مرحله ۲: تست دستی اسکریپت (قبل از تنظیم کرون)
```bash
/usr/bin/php /home/USERNAME/دامنه-شما.ir/cron/logs-cleanup-cron.php
```
به‌جای `/usr/bin/php` مسیری که در مرحله‌ی ۱ پیدا کردید را بگذارید، و به‌جای `/home/USERNAME/دامنه-شما.ir` مسیر واقعی پوشه‌ی پروژه روی هاست.

اگر درست کار کند، خروجی‌ای شبیه این می‌بینید:
```
site_visits: removed 142 rows older than 7 days
activity_logs: removed 8 rows older than 30 days
```
اگر پیام خطا دیدید (مثلاً `Fatal error` یا مشکل اتصال دیتابیس)، قبل از رفتن به مرحله‌ی بعد باید حلش کنید — چون کرون هم دقیقاً همین خطا را می‌گیرد، فقط جایی نمی‌بینیدش مگر ایمیل خطا فعال باشد (مرحله‌ی ۴).

### مرحله ۳: ساخت Cron Job در cPanel
1. وارد cPanel شوید → بخش **Cron Jobs**.
2. پایین صفحه، فرم «Add New Cron Job» را پر کنید:
   - **Minute**: `0`
   - **Hour**: `4` (یعنی ساعت ۴ بامداد؛ یک زمان کم‌ترافیک انتخاب کنید)
   - **Day, Month, Weekday**: `*` (یعنی هر روز)
   - **Command**:
     ```
     /usr/bin/php /home/USERNAME/دامنه-شما.ir/cron/logs-cleanup-cron.php
     ```
     (همان مسیرهایی که در مرحله‌ی ۱ و ۲ تست کردید)
3. روی **Add New Cron Job** بزنید.

نتیجه باید شبیه این باشد:
```
0 4 * * *  /usr/bin/php /home/USERNAME/دامنه-شما.ir/cron/logs-cleanup-cron.php
```

### مرحله ۴: (پیشنهادی) فعال کردن ایمیل نتیجه‌ی کرون
بالای همان صفحه‌ی Cron Jobs، یک فیلد **Cron Email** هست — اگر یک ایمیل توش بگذارید، هر بار که کرون اجرا شود (چه موفق چه با خطا)، خروجی/خطای اسکریپت برایتان ایمیل می‌شود. برای هفته‌ی اول که تازه راه‌اندازی کرده‌اید توصیه می‌شود فعال باشد تا مطمئن شوید همه‌چیز درست کار می‌کند؛ بعداً اگر ایمیل‌های روزانه اذیت‌کننده شد، می‌توانید خالی‌اش کنید.

### مرحله ۵: بررسی چند روز بعد
چند روز بعد از فعال‌سازی، دوباره به Terminal بروید و بررسی کنید که جدول‌ها واقعاً هرس شده‌اند:
```bash
mysql -u DB_USER -p DB_NAME -e "SELECT COUNT(*) FROM site_visits;"
mysql -u DB_USER -p DB_NAME -e "SELECT COUNT(*) FROM activity_logs;"
```
اگر تعداد ردیف‌ها به‌جای رشد پیوسته، ثابت/محدود مانده باشد، یعنی کرون درست کار می‌کند.

### نکات مهم
- **این پاک‌سازی برگشت‌ناپذیر است.** ردیف‌های پاک‌شده دیگر قابل بازیابی نیستند مگر از روی بک‌آپ دیتابیس. اگر هاست شما بک‌آپ خودکار دیتابیس دارد (اکثر هاست‌های cPanel دارند)، جای نگرانی نیست؛ در غیر این‌صورت پیشنهاد می‌شود قبل از فعال‌سازی یک بک‌آپ دستی از دیتابیس بگیرید (یا از بخش پشتیبان‌گیری خودِ پنل ادمین — بخش ۱۳).
- اگر به هر دلیلی خواستید موقتاً این پاک‌سازی متوقف شود، کافی است از cPanel → Cron Jobs، ردیف مربوطه را **Delete** کنید؛ داده‌های قبلی دست‌نخورده می‌مانند.
- تغییر دادن عدد `SITE_VISITS_RETENTION_DAYS` یا `ACTIVITY_LOGS_RETENTION_DAYS` فقط روی اجرای *بعدی* کرون اثر می‌گذارد؛ چیزی که قبلاً پاک شده برنمی‌گردد.

---

## ۱۱. نقشه‌ی سایت، سئو و robots.txt

سایت یک نقشه‌ی سایت پویا دارد که همیشه خودش را با محتوای واقعی دیتابیس به‌روز نگه می‌دارد و نیازی به ساخت دستی ندارد:
```
https://دامنه-شما.ir/sitemap.php
```

فایل `robots.txt` از قبل درون خودش آدرس این نقشه‌ی سایت را دارد:
```
Sitemap: https://rasateams.ir/sitemap.php
```
⚠️ **اگر دامنه‌ی شما چیزی غیر از `rasateams.ir` است، حتماً همین یک خط را داخل `robots.txt` با دامنه‌ی واقعی خودتان جایگزین کنید**، وگرنه گوگل نقشه‌ی سایت را از روی دامنه‌ی اشتباه می‌خواند.

بعد از انتشار سایت، پیشنهاد می‌شود آدرس `sitemap.php` را در [Google Search Console](https://search.google.com/search-console) هم ثبت کنید تا ایندکس شدن صفحات سریع‌تر انجام شود.

---

## ۱۲. حالت تعمیرات

وقتی از پنل ادمین → تنظیمات → «سایت»، سوپرادمین «حالت تعمیرات» را فعال می‌کند، یک فایل به اسم `maintenance.flag` در ریشه‌ی سایت ساخته می‌شود. تا وقتی این فایل وجود دارد:
- همه‌ی صفحات سایت (به‌جز خودِ پنل ادمین در `rasa-manager/`، صفحه‌ی `maintenance.php`، فایل‌های خطا، و فایل‌های استاتیک `assets/`, `css/`, `js/`) به‌صورت خودکار به `maintenance.php` هدایت می‌شوند.
- برای غیرفعال کردن، کافی است همان گزینه را در پنل خاموش کنید تا فایل `maintenance.flag` پاک شود.

اگر پروژه را داخل یک زیرپوشه (نه ریشه‌ی اصلی هاست) نصب کرده‌اید، قوانین مربوط به این ریدایرکت در `.htaccess` (بخش `RewriteCond`های مربوط به `maintenance.flag`) باید متناسب با مسیر زیرپوشه تنظیم شوند.

---

## ۱۳. پشتیبان‌گیری و بازیابی

از پنل ادمین → تنظیمات → «پشتیبان‌گیری» می‌توانید نسخه‌ی پشتیبان کامل یا بخشی از دیتابیس و رسانه‌ها (تصاویر آپلودشده) را تهیه و دانلود کنید، یا یک نسخه‌ی پشتیبان قبلی را بازیابی کنید. فایل‌های پشتیبان در پوشه‌ی `rasa-manager/storage/backups/` ذخیره می‌شوند که با `.htaccess` مخصوص همان پوشه از دسترسی مستقیم عمومی محافظت شده است (`Require all denied`).

پیشنهاد می‌شود:
- قبل از هر تغییر بزرگ (مثلاً قبل از فعال‌سازی کرون پاک‌سازی لاگ‌ها، یا قبل از یک به‌روزرسانی دستی کد) یک نسخه‌ی پشتیبان تازه بگیرید.
- علاوه بر این پشتیبان داخلی، اگر هاست شما هم بک‌آپ خودکار روزانه/هفتگی ارائه می‌دهد (اکثر هاست‌های cPanel این را دارند)، آن را هم فعال نگه دارید — دو لایه‌ی پشتیبان همیشه بهتر از یکی است.

---

## ۱۴. چک‌لیست نهایی امنیت قبل از انتشار

پیش از اینکه آدرس سایت را عمومی/تبلیغ کنید، این موارد را یک‌بار مرور کنید:

- [ ] `APP_SECRET` در `config.php` با یک رشته‌ی تصادفی و طولانی جدید عوض شده (نه مقدار پیش‌فرض)
- [ ] اگر بخش ایمیل را فعال کرده‌اید، `MAIL_ENCRYPTION_KEY` هم با یک رشته‌ی تصادفی *متفاوت* از `APP_SECRET` عوض شده
- [ ] پوشه‌ی `install/` کامل از روی هاست حذف شده
- [ ] اگر بخش ایمیل را فعال کرده‌اید، پوشه‌ی `webmail-integration/` از روی هاست اصلی حذف شده (فقط `resa-mail-bridge.php` کنار Roundcube باقی مانده)
- [ ] رمز عبور حساب اولین ادمین (سوپرادمین) قوی و منحصربه‌فرد است
- [ ] SSL/HTTPS روی دامنه فعال و تست شده است
- [ ] رکورد `Sitemap:` داخل `robots.txt` با دامنه‌ی واقعی سایت هماهنگ است
- [ ] اگر بخش ایمیل فعال است، SPF/DKIM/DMARC تنظیم و با mail-tester.com تست شده‌اند
- [ ] کرون پاک‌سازی لاگ‌ها (بخش ۱۰) تنظیم شده
- [ ] یک نسخه‌ی پشتیبان اولیه از دیتابیس/رسانه‌ها گرفته شده (بخش ۱۳)
- [ ] این فایل راهنما (و هر مستند دیگری که نمی‌خواهید عمومی باشد) از روی هاست برداشته شده یا حداقل بیرون از پوشه‌ی عمومی نگه داشته شده

---

## ۱۵. رفع اشکالات رایج

**صفحه‌ی سفید یا خطای 500 بعد از آپلود**
احتمالاً `config.php` هنوز با اطلاعات واقعی دیتابیس پر نشده، یا نسخه‌ی PHP هاست از ۸ پایین‌تر است. از cPanel → **Select PHP Version** نسخه را چک/تغییر دهید. برای دیدن خطای دقیق، می‌توانید موقتاً یک زیردامنه‌ی تستی مثل `test.دامنه-شما.ir` بسازید که در همان خط تشخیص محیط توسعه/تولید داخل `config.php` به‌عنوان دامنه‌ی واقعی شناخته می‌شود مگر به `.local`/`.test` ختم شود — روش امن‌تر برای دیدن خطا، بررسی **Error Log** داخل cPanel است (خطاها همیشه آنجا ثبت می‌شوند حتی وقتی روی صفحه نمایش داده نمی‌شوند).

**فرم تماس یا آپلود تصویر کار نمی‌کند**
دسترسی نوشتن (Write Permission) پوشه‌های `assets/uploads/` و `rasa-manager/storage/backups/` را از File Manager بررسی کنید.

**بعد از فعال‌سازی حالت تعمیرات، پنل ادمین هم قابل دسترسی نیست**
اگر پروژه داخل یک زیرپوشه نصب شده، قوانین `RewriteCond` مربوط به `maintenance.flag` در `.htaccess` را با مسیر همان زیرپوشه هماهنگ کنید (بخش ۱۲).

**تب «ایمیل» در پنل پیغام «تنظیم نشده» می‌دهد**
یعنی مقادیر `CPANEL_HOST`/`CPANEL_USERNAME`/`CPANEL_API_TOKEN` هنوز در `config.php` خالی‌اند — طبیعی است اگر بخش ایمیل (بخش ۸) را هنوز راه‌اندازی نکرده‌اید؛ بقیه‌ی سایت را مختل نمی‌کند.

**ایمیل‌های ارسالی از سایت به اسپم می‌روند**
به بخش ۹ (SPF/DKIM/DMARC) مراجعه کنید.
